package ch.admin.bit.jme.testsupport;

/**
 * A class in a `ch.admin` package that is not an Avro generated type, used to verify that the whitelist can be
 * narrowed: Avro generated types are trusted by type, everything else only through the configured packages/classes.
 */
public class NotAnAvroType {
}
